# Resend

Use for developer-friendly transactional email and product-triggered lifecycle messages.

Agent notes: verify domain authentication, sender address, templates, suppression behavior, and test delivery before production sends.
