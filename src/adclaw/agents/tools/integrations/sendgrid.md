# SendGrid

Use for transactional email, marketing email, templates, and deliverability infrastructure.

Agent notes: verify authenticated domains, IP or pool settings, templates, suppression lists, and test sends before production traffic.
