# Calendly

Use for meeting scheduling, routing links, round-robin assignment, and conversion handoff flows.

Agent notes: verify event types, routing logic, CRM sync, and timezone behavior before publishing links.
